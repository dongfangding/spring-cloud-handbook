server:
  port: 8099
spring:
  cloud:
    nacos:
      # nacos服务地址通过启动脚本指定
      username: ${nacos_username}
      password: ${nacos_password}
      server-addr: ${nacos_server}
  application:
    name: user-service22
  profiles:
    active: dev

logging:
  level:
    root: debug
